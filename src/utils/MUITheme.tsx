import { createTheme } from "@mui/material/styles";

export const eggTheme = createTheme({
  palette: {
    primary: {
      main: '#c53d63',
    },
    secondary: {
      main: '#f50057',
    },
  },
})
